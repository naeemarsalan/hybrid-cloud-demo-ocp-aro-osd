apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-service
  namespace: hybridbank
  labels:
    app: account-service
    app.kubernetes.io/part-of: hybridbank
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: account-service
  template:
    metadata:
      labels:
        app: account-service
        app.kubernetes.io/part-of: hybridbank
        tier: backend
    spec:
      containers:
        - name: account-service
          image: docker.io/naeemarsalan/hybridbank-account-service:latest
          ports:
            - containerPort: 3002
          env:
            - name: CORE_DB_HOST
              value: postgres-core
            - name: CORE_DB_PORT
              value: "5432"
            - name: CORE_DB_USER
              value: hybridbank
            - name: CORE_DB_PASSWORD
              value: hybridbank
            - name: CORE_DB_NAME
              value: hybridbank_core
            - name: ARCHIVE_DB_HOST
              value: postgres-archive
            - name: ARCHIVE_DB_PORT
              value: "5432"
            - name: ARCHIVE_DB_USER
              value: hybridbank
            - name: ARCHIVE_DB_PASSWORD
              value: hybridbank
            - name: ARCHIVE_DB_NAME
              value: hybridbank_archive
          readinessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 15
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: hybridbank
  labels:
    app: account-service
    app.kubernetes.io/part-of: hybridbank
spec:
  selector:
    app: account-service
  ports:
    - port: 3002
      targetPort: 3002
  type: ClusterIP
